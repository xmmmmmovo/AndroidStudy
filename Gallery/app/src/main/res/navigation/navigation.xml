<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/gallrtyFragment">

    <fragment
        android:id="@+id/gallrtyFragment"
        android:name="com.nuc.gallery.GallrtyFragment"
        android:label="Gallery"
        tools:layout="@layout/fragment_gallrty">
        <action
            android:id="@+id/action_gallrtyFragment_to_pagerPhotoFragment"
            app:destination="@id/pagerPhotoFragment" />
    </fragment>
    <fragment
        android:id="@+id/pagerPhotoFragment"
        android:name="com.nuc.gallery.PagerPhotoFragment"
        android:label="pagerPhoto"
        tools:layout="@layout/fragment_pager_photo" />
</navigation>