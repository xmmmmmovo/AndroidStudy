<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/gallrtyFragment">

    <fragment
        android:id="@+id/gallrtyFragment"
        android:name="com.nuc.gallery.GallrtyFragment"
        android:label="Gallery"
        tools:layout="@layout/fragment_gallrty">
        <action
            android:id="@+id/action_gallrtyFragment_to_photoFragment"
            app:destination="@id/photoFragment" />
    </fragment>
    <fragment
        android:id="@+id/photoFragment"
        android:name="com.nuc.gallery.PhotoFragment"
        android:label="Photo"
        tools:layout="@layout/fragment_photo" />
</navigation>